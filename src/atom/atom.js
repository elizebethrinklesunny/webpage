import {
    atom,
  } from 'recoil';

export const Todolistdata = atom({
    key: 'Todolistdata', // unique ID (with respect to other atoms/selectors)
    default: false, // default value (aka initial value)
  });


